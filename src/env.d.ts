/** Injected by vite.config.ts: package version + short git SHA of the build. */
declare const __APP_VERSION__: string;
/** Injected by vite.config.ts: UTC date of the build, YYYY-MM-DD. */
declare const __BUILD_DATE__: string;
